def process_data(d):
    k = d.keys() # Variable k unused effectively or poorly named
    # mixed issues: spacing, no docstring, type
    res=[]
    for i in d:
        res.append(str(i))
    return res
